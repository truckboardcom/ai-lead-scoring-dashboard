# AI Lead Scoring Dashboard - Auto Updater

## 🚀 Установка и настройка

### 1. Установка зависимостей

```bash
pip install python-dotenv
# Все остальное - стандартная библиотека Python
```

### 2. Конфигурация FTP

Скопируйте `.env.example` в `.env` и заполните ваши данные:

```bash
cp .env.example .env
nano .env
```

Заполните:
- `FTP_HOST` - адрес вашего FTP сервера
- `FTP_USER` - имя пользователя FTP
- `FTP_PASSWORD` - пароль FTP
- `FTP_REMOTE_PATH` - путь к папке на сервере (например: `/public_html/dashboard/`)
- `FTP_USE_TLS` - использовать ли TLS (true/false)

### 3. Первая загрузка

Запустите скрипт вручную:

```bash
python3 ftp_updater.py
```

### 4. Автоматическое обновление (Cron)

Для автоматического обновления каждые 30 минут добавьте в crontab:

```bash
crontab -e
```

Добавьте строку:

```
*/30 * * * * cd /path/to/your/scripts && python3 ftp_updater.py >> /var/log/dashboard_update.log 2>&1
```

### 5. Альтернатива: Systemd Timer

Создайте файл `/etc/systemd/system/dashboard-updater.service`:

```ini
[Unit]
Description=AI Lead Scoring Dashboard Updater
After=network.target

[Service]
Type=oneshot
User=your_user
WorkingDirectory=/path/to/your/scripts
ExecStart=/usr/bin/python3 /path/to/your/scripts/ftp_updater.py
Environment="PYTHONUNBUFFERED=1"

[Install]
WantedBy=multi-user.target
```

Создайте таймер `/etc/systemd/system/dashboard-updater.timer`:

```ini
[Unit]
Description=AI Lead Scoring Dashboard Update Timer
Requires=dashboard-updater.service

[Timer]
OnBootSec=5min
OnUnitActiveSec=30min
Unit=dashboard-updater.service

[Install]
WantedBy=timers.target
```

Активируйте:

```bash
sudo systemctl daemon-reload
sudo systemctl enable dashboard-updater.timer
sudo systemctl start dashboard-updater.timer
```

## 📱 Доступ с телефона

После загрузки дашборд будет доступен по адресу:
```
http://your-domain.com/dashboard/
```

Дашборд оптимизирован для:
- ✅ Мобильных устройств (iOS, Android)
- ✅ Планшетов
- ✅ Десктопов
- ✅ Темной темы (по умолчанию)

## 🔗 Интеграция с Kommo CRM

Кнопка "🚀 Открыть CRM" ведет напрямую в Kommo.
Кнопка "👁️ Открыть в CRM" на каждом лиде ведет к конкретному лиду.

URL формат: `https://golzpartners.kommo.com/leads/detail/{LEAD_ID}`

## 🔄 Обновление данных

Дашборд автоматически обновляется:
1. **Через FTP**: Каждые 30 минут (настраивается)
2. **В браузере**: Авто-обновление каждые 5 минут
3. **Вручную**: Кнопка "🔄 Обновить"

## 📊 Структура файлов

```
dashboard/
├── index.html              # Главный дашборд
├── ftp_updater.py         # Скрипт обновления
├── .env                   # Конфигурация (НЕ КОММИТИТЬ!)
├── .env.example           # Пример конфигурации
└── README.md              # Эта инструкция
```

## 🛠️ Troubleshooting

### Ошибка подключения к FTP

Проверьте:
1. Правильность хоста и порта
2. Логин и пароль
3. Разрешен ли FTP на сервере
4. Нужен ли TLS (попробуйте `FTP_USE_TLS=false`)

### Файлы не обновляются

1. Проверьте права на папку на сервере
2. Проверьте путь `FTP_REMOTE_PATH`
3. Проверьте логи: `tail -f /var/log/dashboard_update.log`

### Дашборд не отображается корректно

1. Очистите кэш браузера
2. Проверьте, что файл загружен полностью
3. Откройте консоль браузера (F12) для ошибок

## 🎨 Кастомизация

Вы можете изменить:
- Цвета в CSS переменных (`:root` в `<style>`)
- Количество отображаемых лидов
- Фильтры и сортировку
- Интервал авто-обновления

## 📞 Поддержка

По вопросам обращайтесь к вашему администратору или разработчику.

---

**Версия**: 1.0.0  
**Дата создания**: 20.11.2025  
**Powered by**: Composio + AI
